import Typography from "@mui/material/Typography";
import Container from "@mui/material/Container";

const Account = () => {
  return (
    <Container
      sx={{
        flex: "1 0",
        minHeight: "100vh",
      }}
    >
      <Typography variant="h3" align="center">
        <strong>Account Login Feature Coming Soon!</strong>
      </Typography>
    </Container>
  );
};

export default Account;
